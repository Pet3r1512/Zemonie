{
    "name": "zemonie",
    "private": true,
    "scripts": {
        "dev": "concurrently \"pnpm --dir client dev --host\" \"pnpm --dir server dev\"",
        "test:client": "pnpm --dir client test"
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}